---
- name: Install basic packeges
  apt:
    name: "{{ item }}"
    update_cache: yes
    autoremove: yes
  with_items:
    - iotop
    - htop
    - mc
    - net-tools
  when: ansible_os_family == "Debian"